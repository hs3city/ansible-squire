---
- name: create application data directory
  file:
    state: directory
    path: /var/eph/
  become: yes

- name: create application config directory
  file:
    state: directory
    path: /etc/svc/
  become: yes

  #- name: copy application configs
  #  file:
  #    state: directory
  #    path: /var/eph/{{ item.app }}
  #    owner: "{{ item.user }}"
  #  loop: []
  #  become: yes
